fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.1
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
redis==8.1.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
structlog==26.1.0
orjson==3.12.0

# --- Phase 2: live streaming engine --------------------------------------
httpx==0.28.1
python-multipart==0.0.20
# OpenCV headless for the motion-snapshot pipeline (frame difference only)
opencv-python-headless==4.11.0.86

# --- Phase 3: AI inference engine -----------------------------------------
# CPU ONNX Runtime. Swap for `onnxruntime-gpu` on production GPU hosts; the
# GpuScheduler auto-falls-back to CPU when CUDA providers are absent.
onnxruntime>=1.20.1
# pynvml (optional, unpinned): NVIDIA management for `gpu_usage()` telemetry.

# --- Phase 6.2: Enterprise security & production hardening -----------------
# MFA (TOTP) + QR provisioning for /security/mfa enrollment.
PyOTP==2.10.0
qrcode==8.2
# Pillow is a qrcode dependency for PNG/b64 provisioning images.
pillow==12.3.0
# In-memory SQLite driver used by test fixtures (sqlite+aiosqlite:///:memory:).
aiosqlite==0.22.1

# --- Dev / validation tooling ---------------------------------------------
ruff==0.9.10
pytest==8.3.4
pytest-asyncio==0.25.3