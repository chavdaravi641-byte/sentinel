# Internal backend URL used by the Next.js rewrite proxy.
# In docker-compose this is overridden to http://api:8000
API_BACKEND_URL=http://localhost:8000

# Optional: exposed to the browser if you ever need direct API base.
NEXT_PUBLIC_API_BASE=/api